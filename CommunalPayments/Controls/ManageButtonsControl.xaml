<UserControl x:Class="CommunalPayments.Controls.ManageButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:CommunalPayments.ViewModels.Controls" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ManageButtonsControlViewModel}"
             mc:Ignorable="d">
    <Grid>
        <StackPanel x:Name="ControlButtons">
            <Button x:Name="SaveCalling" Content="Сохранить" Command="{Binding SaveIndicatorCommand}"/>
            <Button x:Name="SettingsCalling" Content="Настройки" Command="{Binding CallSettingsCommand}"/>
            <Button x:Name="InfoCalling" Content="О программе" Command="{Binding CallApplicationInfoCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
